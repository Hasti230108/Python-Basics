# Day 24 - NumPy Statistics

This file contains my practice with NumPy statistical and aggregation operations.

## Topics Covered

* `np.sum()`
* `np.mean()`
* `np.min()`
* `np.max()`
* `np.std()`
* 1D array statistics
* 2D array statistics
* Row-wise operations using `axis=1`
* Column-wise operations using `axis=0`

## Practice

Used student marks to calculate:

* Total marks
* Average marks
* Minimum marks
* Maximum marks
* Standard deviation
* Total marks of each student
* Average marks of each student
* Total marks of each subject
* Average marks of each subject

## Key Concept

```text
axis=0 → column-wise operation
axis=1 → row-wise operation
```

## Outcome
Practiced using NumPy to perform basic statistical calculations and analyze data stored in 1D and 2D arrays.